# ── Base de données ──────────────────────────────────────────────────────────
DB_ROOT_PASSWORD=change_this_root_password
DB_NAME=jnm2026
DB_USER=jnmuser
DB_PASSWORD=change_this_db_password

# URL complète (utilisée par Prisma migrate)
DB_URL=mysql://jnmuser:change_this_db_password@mariadb:3306/jnm2026

# ── Authentification ─────────────────────────────────────────────────────────
JWT_SECRET=change_this_jwt_secret_to_something_very_long_and_random

# ── Seeding admin (optionnel) ────────────────────────────────────────────────
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_admin_password

# ── SMTP (envoi d'emails) ─────────────────────────────────────────────────────
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ton@email.com
SMTP_PASSWORD=change_this_smtp_password
SMTP_FROM=noreply@jnm2026.fr
NEXT_PUBLIC_SITE_URL=https://ton-domaine.fr
